name: MAE_IXI_BraTS
target_size: &target_size !!python/tuple [128, 128]
train_batch_size: &tbatch_size 32
downstream_batch_size: &dbatch_size 1
num_workers: &num_workers 8
checkpoint_path: &checkpoint_path ./weights/IXI_BraTS/mae/

experiment:
  name: IXI_BraTS_AnomalyDetection
  task: train
  weights:

device: gpu
configurator:
  module_name: core.Configurator
  class_name: DLConfigurator
  params:

#
# Model - MAE (Masked Autoencoder)
#
model:
  module_name: model_zoo.mae
  class_name: MaskedAutoencoderViT
  params:
    img_size: 128
    patch_size: 16
    embed_dim: 512
    depth: 12
    num_heads: 16
    decoder_embed_dim: 512
    decoder_depth: 8
    decoder_num_heads: 16
    mlp_ratio: 4

trainer:
  module_name: projects.24_normative_eval.trainers.MAE_Trainer
  class_name: PTrainer
  params:
    input_size: *target_size
    checkpoint_path: *checkpoint_path
    nr_epochs: 1500
    patience: 500
    loss:
      module_name: optim.losses.ln_losses
      class_name: L2
      params:
    optimizer_params:
      lr: 1.0e-4
  data_loader:
    module_name: core.DataLoader
    class_name: DefaultDataLoader
    params:
      args:
        dataset_module:
          module_name: data.loaders.brain_loader
          class_name: BrainLoader
        data_dir:
          train:
            - './data/IXI_BraTS/splits/ixi_train.csv'
          val:
            - './data/IXI_BraTS/splits/ixi_val.csv'
          test:
            - './data/IXI_BraTS/splits/ixi_val.csv'
        target_size: *target_size
        batch_size: *tbatch_size
        num_workers: *num_workers

downstream_tasks:
  brats_detection:
    module_name: projects.24_normative_eval.DownstreamEvaluator
    class_name: PDownstreamEvaluator
    checkpoint_path: *checkpoint_path
    params:
      task: 'detection'
    data_loader:
      module_name: core.DataLoader
      class_name: DefaultDataLoader
      params:
        args:
          target_size: *target_size
          batch_size: *dbatch_size
          dataset_module:
            module_name: data.loaders.brain_loader
            class_name: BrainLoader
          data_dir:
            test:
              - './data/IXI_BraTS/splits/brats_test.csv'
            mask:
              - './data/IXI_BraTS/splits/brats_test_masks.csv'
            neg:
              - './data/IXI_BraTS/splits/brats_test_neg.csv'
